---
sidebar_position: 1
---

# Introduction

This documentation site is managed by Docusaurus and registered as the `docs` Nx application.

Use the Nx targets from the workspace root:

```bash
yarn nx serve docs
yarn nx build docs
yarn nx typecheck docs
```
