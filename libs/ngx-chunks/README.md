# ngx-chunks

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ngx-chunks` to build the library.

## Running unit tests

Run `nx test ngx-chunks` to execute the unit tests via [Vitest](https://vitest.dev/).
